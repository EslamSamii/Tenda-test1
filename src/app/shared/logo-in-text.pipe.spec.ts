import { LogoInTextPipe } from './logo-in-text.pipe';

describe('LogoInTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LogoInTextPipe();
    expect(pipe).toBeTruthy();
  });
});
